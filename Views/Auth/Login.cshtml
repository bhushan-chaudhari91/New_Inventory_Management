@using InventoryManagement.ViewModels;
@model UserInfoViewModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">


    <style>
        body, html {
            height: 100%;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f2f5;
        }

        .login-container {
            background-color: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            width: 100%;
            text-align: center;
        }

        .brand-card {
            background-color: #5192f1;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 20px;
            font-weight: bold;
            color: #ffffff;
        }

        h2 {
            margin-bottom: 20px;
            color: #333;
            font-weight: bold;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-control {
            border-radius: 5px;
            padding: 10px;
            border: 1px solid #ccc;
        }

        .btn-primary {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            background-color: #0d6efd;
            border: none;
            font-weight: bold;
            transition: background 0.3s ease;
        }

            .btn-primary:hover {
                background-color: #335b97;
            }

        .alert {
            text-align: center;
            margin-top: 15px;
            padding: 10px;
        }

    </style>
</head>

@* <body>
    <div class="login-container">
        <h2>Venus Precious Metal Refinery</h2>

        <h2>Login</h2>
        <form asp-action="Login" method="post">
            <div class="form-group">
                <label for="username">Username</label>
                <input asp-for="UserName" class="form-control" id="username" placeholder="Enter your username" required />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input asp-for="Password" type="password" class="form-control" id="password" placeholder="Enter your password" required />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Login</button>
                @Html.ValidationSummary(false)
            </div>

            @if (ViewData.ModelState.ErrorCount > 0)
            {
                <div class="alert alert-danger">
                    Invalid login attempt. Please try again.
                </div>
            }
        </form>
    </div>

</body> *@

<body>
    <div class="login-container">

        @* <a class="navbar-brand brand-logo-mini">
            <img src="~/images/Logo image.png"
                 alt="logo" style="width: 60%; margin-top: -30px;" />
        </a> *@

        <div class="brand-card">
            Inventory Management
        </div>

        <h2>WELCOME</h2>
        <form asp-action="Login" method="post">
            <div class="form-group">
                <label for="username">Username</label>
                <input asp-for="UserName" class="form-control" id="username" placeholder="Enter your username" required />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input asp-for="Password" type="password" class="form-control" id="password" placeholder="Enter your password" required />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Log In</button>
                @Html.ValidationSummary(false)
            </div>

            @if (ViewData.ModelState.ErrorCount > 0)
            {
                <div class="alert alert-danger">
                    Invalid login attempt. Please try again.
                </div>
            }
        </form>
    </div>
</body>
</html>
